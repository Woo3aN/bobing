# 项目阅读指南

> 写给"只学过 C/C++ 和一点数据结构"的你。
> 目标不是"读完 6000 行"，而是**能自己改一处、看懂一处效果**——按下面的顺序，一块一块啃。

---

## 全局地图（先看这个，别急着读代码）

| 文件 | 行数 | 是什么 | 难度 |
|---|---|---|---|
| `博饼.html` | 6044 | **游戏本体**：HTML + CSS + JS 全在一个文件里 | ★★☆ |
| `cf/mock-server.js` | 319 | 本地房间服务（联机调试用，跑起来最简单） | ★★☆ |
| `cf/src/worker.js` | 476 | 线上房间服务（Cloudflare Worker + Durable Object） | ★★★ |
| `tools/gen_bowl.js` | 594 | 碗的几何生成器（**纯数学**，无界面） | ★☆☆ |

**`博饼.html` 内部是按 `====` 分隔的章节写的**，直接搜索 `/* ====` 能列出所有章节标题——
这是你读这份文件的主要导航方式，别从第 1 行顺着读。

**建议顺序**：`gen_bowl.js`（纯数学，你最熟）→ `博饼.html` 的规则部分 → 界面 → 联机 → 测试。
**联机放最后**，它涉及"网络 + 异步 + 分布式状态"，是要一层层垫上去的。

---

## 第 0 步：先把它跑起来，再"看见"内部状态

打开游戏，按 **F12** 打开控制台，输入：

```js
__game.state()          // 打印当前整局状态（一个普通 JS 对象，像一份存档）
__game.rollRandom()     // 用随机骰子掷一把
__game.rollForced([4,4,4,4,5,6])   // 指定骰子掷一把（调试神器）
__game.newGame({ players: [{name:'A'},{name:'B'}], fast: true })  // 重开一局
```

URL 参数也能造场景：`博饼.html?demo=6&solo=1` = 直接进单人、先博 6 把。

> 这一步的意义：**你能随时"看到"程序内部的数据**。读代码时遇到不懂的，就打开控制台打印出来看。
> 这是读前端代码最重要的手段——比在 C 里用 gdb 还方便。

---

## 第 1 步：一次掷骰（纯逻辑，你最熟，从这里开始）

从 URL 参数到一次掷骰，链路是：

```
randomDice()   → evaluate(dice)  → doRoll(dice, silent)  → afterRoll()  → 渲染
  产生骰子          判定等级            写状态 + 加日志       推进流程       刷新界面
  第 1470 行        第 979 行          第 1475 行            第 1735 行
```

**先读 `evaluate`（979 行）**——这就是你的强项所在：

- 它接收 6 个点数 `[1..6]`，返回一个"等级对象"（名称 / 大小 tier / 带点 kicker）
- 实现思路是**按状元级从高到低逐条判断**，和你在数据结构课学的"分类讨论"一样
- `compareGrade(a, b)`（1015 行）就是比较函数：先比 `tier`，再比 `kicker`——**和你写 C 的 `qsort` 比较函数是同一个思路**

**动手练习**（强烈建议做，20 分钟）：

```bash
node -e "
$(sed -n '974,1013p' 博饼.html)     // 把 evaluate 附近整段抠出来
for (let a=1;a<=6;a++) for (let b=1;b<=6;b++) for (let c=1;c<=6;c++)
for (let d=1;d<=6;d++) for (let e=1;e<=6;e++) for (let f=1;f<=6;f++) {
  const g = evaluate([a,b,c,d,e,f]);
  cnt[g.key] = (cnt[g.key]||0)+1;
}
console.log(cnt);   // 穷举 46656 种组合，看看每种奖项各占多少
"
```

跑出来你会得到和游戏里"概率"面板一模一样的数据——**这时你就真正理解"为什么一秀是 37.29%"了**。

---

## 第 2 步：游戏状态（数据结构，也是你熟的）

`state`（定义在 1272 行，`newGame` 在 1389 行）就是**一份游戏存档**，关键字段：

| 字段 | 含义 |
|---|---|
| `players[]` | 玩家数组，每人有 `name` / `cakes`（各奖项几份）/ `zyLast`（他最后博出的状元） |
| `pool` | 各奖项**还剩几份**（比如一秀 32 份，被谁博走就减一） |
| `current` | 现在轮到谁（数组下标） |
| `rollCount` | 这一局已经博了多少把 |
| `phase` | `playing`（进行中）/ `final`（收官轮）/ `over`（结束） |
| `log[]` | 博饼记录（`unshift` 往头部插，所以 `log[0]` 是最新的） |
| `zyHolder` / `zyGrade` | 状元现在归谁 / 是什么等级 |

**规则的核心**都在 `doRoll` 和 `afterRoll` 里：

- 博到奖项 → 从 `pool` 扣一份，记到该玩家的 `cakes`
- **状元特殊**：不扣池子，而是"谁大归谁"（`compareGrade`），同样大则先博者保留
- **收官轮**：最后一个奖项有了主 → `phase` 变 `final`，从下家起每人再博一次

看懂 `phase` 怎么流转，你就掌握了整个游戏流程。

---

## 第 3 步：界面怎么跟着变（需要补一点前端）

这个项目的渲染方式很朴素，**对 C 背景的人反而好懂**：

```
数据变了  →  用字符串拼出 HTML  →  innerHTML 塞进去
```

对应函数：`renderPlayers`（2465）、`renderLog`（2490）、`updateRollButton`（2520）。

- 没有 React/Vue 那套"响应式框架"，全是手写的
- 所以有个坑它吃过：**"数据变了但忘了重新渲染"** → 界面就和实际脱节（项目里到处是这种注释）
- 想改界面：找到对应 `render*` 函数，改里面的 HTML 字符串就行

**要补的知识**（按需查，别系统学）：
- **CSS Flex/Grid**：管排列（搜索 `.layout`、`.topbar`）
- **内联 SVG**：碗和骰子是画出来的，`<path>` 的 `d` 属性就是"坐标点串"——**本质是几何，你会喜欢**
- **`tools/gen_bowl.js`（594 行）**：给定相机角度和碗的尺寸，算出碗沿的投影椭圆。
  注释里写了公式（`sinθ`、`cosθ`），是纯数学，**读它不需要任何前端知识**——建议从这里建立信心。

---

## 第 4 步：联机（最难，最后看）

**先补一个思维模型**：这个游戏联机用的是**事件回放**——

> 服务器不存"牌局结果"，只存**一串操作**（"第 1 把 A 掷出 4,4,4,4,5,6"）。
> 每台设备拿到同一串操作，各自在本地算一遍，结果必然一致——**像看录像，而不是传输结果**。

理解了这句，再看代码就轻松了：

1. **`cf/mock-server.js`（319 行）**：先读它。它就是"房间服务"的完整实现（内存版），
   消息格式、校验逻辑与线上完全一致，但**没有 Cloudflare 的概念**，容易看懂。
2. **`cf/src/worker.js`（476 行）**：再看它。跟 mock 是同一套逻辑，换成了 Cloudflare 的
   Durable Object（可以理解成"一个有状态、带数据库、能收 WebSocket 的服务对象"）。
3. **客户端侧**：`var NET = (function () {...})` 在 3284 行，里面是四个可插拔的传输层
   （线下的 Local、线上的 WS…）。`beginGame`（3956）、`applyRemoteEvents`（4002）是回放入口。

**联机最难的不是网络，是"人不在了怎么办"**——项目里有一套"托管"逻辑：
15 秒没动就替你博、最多 5 把、2 分钟后算彻底掉线。这部分注释写得很详细
（搜索 `托管`），**读懂它是很好的分布式状态机练习**。

**要补的知识**：WebSocket（一次握手、双向长连接）、异步（`async/await`、回调）、
分布式状态（多个副本如何保持一致）。

---

## 第 5 步：测试（工程习惯）

```bash
bash tools/run_probes.sh        # 全套浏览器自检（13 组 481 项断言）
cd cf && node mock-server.js    # 另开终端起本地房间服务
WS_URL=ws://127.0.0.1:8911/ws node test-room.js   # 协议测试 59 项
```

读 `test-room.js` 能学到"怎么用代码验证一个网络协议"——**这是学校里不太教、但工作里天天用的东西**。
探针里还有 `A/B 反证`（比如"故意把 id 改错，确认断言会失败"），**这是保证测试本身可信的方法**。

---

## 附录：JS 与 C/C++ 的关键差异（你最容易卡的地方）

| 概念 | C/C++ | 这个项目里的 JS | 提示 |
|---|---|---|---|
| 变量声明 | `int x;` | `var x` / `let` / `const` | 项目里大量用 `var`，作用域是**函数级**的 |
| 类型 | 静态、要声明 | **动态**，变量只是"名字" | `state.players[i].cakes` 一路点下去就行，不用管类型 |
| 对象 | `struct` | **对象 ≈ 字典 / map** | `{a:1,b:2}` 类似 `unordered_map`；可随时加字段 |
| 函数 | 独立 | **函数是一等公民**（可当参数传） | `setTimeout(fn, 100)` 就是传函数 |
| 闭包 | 函数指针+静态变量 | **闭包能捕获外部变量** | `var NET = (function(){...})()` 这种"立即执行函数"就是靠闭包做私有变量 |
| 数组 | 连续内存 | `[]` 是**可伸缩数组**（≈ `vector`） | `arr.push(x)`、`arr.unshift(x)` |
| 字符串 | `char[]` | 天生是字符串，可直接 `+` 拼接 | `'第' + n + '把'` |
| 异步 | 线程/回调 | **单线程 + 事件循环** | 没有真并发；`await` 是"等结果，但不阻塞别人" |
| 报错 | 返回值/errno | **抛异常**（可被 `catch` 吞掉！） | 项目里踩过一个坑：异常被静默 catch，导致 bug 难查 |
| 类 | `class` | 原型（`prototype`） | **本项目几乎不用 class**，都是普通对象 + 函数，更好读 |

**最需要适应的三点**：① 动态类型（不用管类型，但写错了运行时才炸）② 函数是一等公民（会被传来传去）
③ 异步（理解"事件循环"比什么都重要）。

---

## 读代码的方法（比顺序更重要）

1. **不要从第一行读**。用功能入口反着找：想懂"概率怎么算"就去 `evaluate`。
2. **改一行看效果**。改 `evaluate` 里某个判定，刷新页面看概率面板变化——**反馈极快**。
3. **`console.log` 是主力武器**。项目里预留了 `__game` 一堆钩子（文件末尾有说明）。
4. **注释比代码值钱**。作者踩过的坑都写在注释里了（"⚠️"开头那些）——**那些是这份代码里最有价值的部分**。
5. **一次只啃一块**。6044 行不是用来"读完"的，是用来"按需查"的。
6. **改坏了不要紧**：动手前 `cp 博饼.html 博饼.html.bak` 备份一份，随便折腾。

---

**一句话总结**：从 `evaluate`（规则）和 `gen_bowl.js`（几何）入手——那是纯逻辑和数学，你已经会了；
界面和联机只是"把结果画出来"和"让多台设备看到同一份结果"，遇到不懂的概念现查就行。
---

## 顺带聊聊：AI 时代该从这个项目学什么

> 这是答"大二学生该学什么"时写的一节，放在这里备查。

### AI 抢走的是"翻译"，抢不走"判断"

AI 能写 CRUD、能拼 CSS、能调 API——**这类"把已知需求翻译成语法"的活，确实不用人做了**。它做不了的是：

1. **需求本身**："状元插金花最大""15 秒没动就托管""最多 5 把"——这些数字是**产品判断**，不是代码问题。
   AI 能写出托管逻辑，写不出"为什么是 15 秒"。
2. **信息不全时的决策**：真机上"进不了观战"，根因是"页面被杀 → 身份丢失 → 按名字认领"这条路径——
   这种带真实世界噪声的问题，得从现象一步步推理。
3. **判断"它对不对"**：AI 写的代码**看起来都对**。能验证的人稀缺。

### 这个项目里最值钱的几处

| 学什么 | 在哪 | 为什么值钱 |
|---|---|---|
| **读注释里的坑** | 所有 `⚠️` 开头的注释 | AI 生成的代码几乎没有这种经验沉淀——它没踩过 |
| **验证能力** | `tools/run_probes.sh`（481 断言）、`test-room.js`（59 项）、A/B 反证 | AI 写得快，**能验证对不对的人稀缺** |
| **状态机与一致性** | `cf/src/worker.js` 托管逻辑 | "重连"与"认领"是两条路，只改一条就部分设备坏——分布式经典陷阱 |
| **成本倒逼设计** | "全员挂机时停止写事件" | 免费额度 → 设计决策，是工程判断不是语法 |
| **架构取舍** | 为什么服务端不跑规则（事件回放） | 换来零成本、代价是没有防作弊——**没有最优解，只有取舍** |

**反面教材**：开发过程中 AI（就是我）在这个项目里三次拿不可靠的测量当依据——
用元素宽度加总判断"放得下"（换行后宽度不变，永远"看着够"）、
用 `--window-size` 截图当判据（实际视口比标称窄）、
断言"所有手机都折行"（被用户一台 Pro Max 当场推翻）。
**每一次都"看起来很有把握"，每一次都错**——这类教训都在上面的注释里。

### 具体建议

**别花时间**：某框架的 API 用法、页面怎么写好看（AI 一秒给你，两年就过时）。

**要花时间**：
1. **基础课照学，换个角度**：操作系统、网络（TCP/HTTP/WebSocket 到底怎么走）、数据库（事务/索引）
   —— 这些是 AI 的黑盒之外，**C/C++ 的底子在这里是优势**。
2. **加一门系统设计**：怎么拆模块、怎么权衡成本与复杂度。AI 能实现，**架构是你定的**。
3. **练"从现象到根因"**：挑一个 bug（如"取消托管点了没反应——消息根本没发出去，异常被静默 catch 吞掉"），
   只看现象自己推一遍，再对答案。
4. **给现有功能补测试**：会不断发现"以为对其实错"的地方，这是最有效的学习方式。
5. **动手改一个真需求**：比如"每局轮换先手"——规则理解 + 联机一致性 + 测试，完整的工程练习。

**一句话**：AI 让你**不必会写**，但让你**必须会判断**。别学那 6000 行怎么写，
学**设计背后的取舍**：为什么这么选、那些数字怎么来的、那个 bug 为什么找了两天。
