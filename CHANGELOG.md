# 更新日志

中秋博饼从零开始的完整演进记录。
**日志最新版本：v2026.09.24** ｜ **游戏构建版本（联机面板底部显示）：v2026.09.24**
（构建版本只在游戏代码变动时更新；文档、素材、仓库设置这类改动记日志但不改构建号。）

---

## v2026.09.24 · 夜间模式配色修复

### 变更

- 游戏构建版本 `NET_BUILD` 从 `v2026.09.23b` 升到 `v2026.09.24`——本次改动了 `index.html`，
  按本文件开头的规则同步构建号（方便对照确认 WebView 没跑旧缓存）。

### 修复

- **夜间模式（强制深色 / 反色）下骰子变黑、红金配色失真**。
  根因：本页是**自带的暗色设计**（深红碗 + 鎏金 + 象牙白骰子），但全篇从未声明过配色偏好，
  于是浏览器与反色类扩展会按「浅色页」的逻辑把它整体反色 —— 骰面那条象牙白渐变
  （`linear-gradient(150deg,#fffefb…#eee3c9)`）被反成近黑，红/金也被拽向奇怪色相。
  补上三道标准退出开关，缺一不可：

  | 开关 | 作用对象 | 说明 |
  |---|---|---|
  | `<meta name="color-scheme" content="only light">` | Chrome / Edge 的 Auto Dark Mode | 官方唯一认可的退出方式；不写这行，手机端系统深色模式下一律强制反色 |
  | `<meta name="darkreader-lock">` | Dark Reader 等反色扩展 | 扩展侧识别的退出标记 |
  | 样式表首尾 `color-scheme:only light` + `@media (prefers-color-scheme:dark)` 兜底块 | 只认 CSS 不认 meta、或只认 `prefers-color-scheme` 的旧内核 / 国产浏览器「智能深色」 | 把页面底色、骰面、点数三处配色按设计原值再钉一遍（骰面用的是写死的 hex 渐变而非变量，必须整个重写） |

  ⚠️ `only light` **不是**「浅色主题」的意思，它说的是「本页只按浅色规则渲染，请勿自动反色」——
  本页依然是暗色设计，观感不变。

- 改动为**纯新增**，未修改任何既有规则：修复前 / 修复后各视口截图逐字节一致（md5 相同），
  页面内置自检 158 项断言全过（dice 69 / exhaustive 34 / rules 35 / flow 20）。

### 仓库 / 部署（不改构建号）

- **`cf/wrangler.jsonc` 补回 `run_worker_first`**：线上一直开着
  `["/", "/bobing", "/bobing/*", "/ws"]`，仓库里却漏了这项——照仓库配置部署会让静态资源
  优先命中，`/` 直接吐出 `index.html`，Worker 里写好的 `302 → /bobing` 失效（主页留白被破坏）。
  此前几次部署是在本地改配置后未提交，所以 git 与线上长期不一致。
- **`cf/test-room.js`、`cf/mock-server.js` 去掉写死的本机绝对路径**（另一台电脑的 `ws`
  模块路径、以及 `public` 目录路径），改用 `require('ws')` 与 `path.join(__dirname, 'public')`，
  换台机器也能直接跑。
- **`tools/run_probes.sh` 的入口改为 `index.html`**：此前指向 `博饼.html`，
  而该文件在本仓库从未出现过（全历史零重命名），所以这个脚本一直跑不通。

### 测试脚本 · 修掉线上的假失败（不改构建号，服务端与游戏均无改动）

对着线上跑 `cf/test-room.js` 时会稳定报错。逐层查下来是**两处测试侧问题**，不是 bug：

- **「跳过在线玩家被拒绝」踩在挂机判定的边界上。** 服务端的设计是「某座次 15 秒没动作
  → 判定挂机、允许别人替其代博/跳过」（在线也算，见 `cf/README.md` 的托管表）。
  而测试跑到这一步耗时早就超过 15 秒，于是那条 skip 被**正当地**接受 → 事件表多出一条 skip
  → 后面按固定下标写的代博断言整体错位。
  已做决定性验证：开局后立刻 skip 被拒 / 静置 16 秒后 skip 被接受 / 掷骰刷新活动后再 skip
  又被拒 —— 三条都符合设计。
  修法：断言前先发一条 `cancel` 刷新该座次的活动时间（cancel 只清托管 + 打时间戳，
  **不产生事件**，所以事件下标不受影响；不能用 roll，因为上一条事件正是同一座次，会被幂等判据拦掉）。

- **`ws` 客户端与 Cloudflare 协商 permessage-deflate 会让个别连接假死。**
  症状：某条 socket 拿到开局快照后**再也收不到任何广播**，而同房间别的连接一切正常，
  于是断言随机地挂在"某个连接的视图滞后"上（同一份代码连跑三次分别报 2 / 1 / 9 条失败）。
  改为 `perMessageDeflate: false` 后 44/44 全过。
  真实客户端用的是浏览器原生 WebSocket，不受此影响（线上游戏一直正常），所以这属于测试客户端的事。

其余为加固：文件头补一条 ⚠️（凡断言"某动作必须被拒绝"，先确保该座次 15 秒内动作过），
代博断言的报错信息补 `iProxy`（按内容定位代博事件，将来一眼能看出下标错位）。

- **代博段"只等一条连接、却断言另一条"**：本段以前只等 `guest` 的视图到位，紧接着就读 `host`
  的视图做断言；线上某条连接的推送偶尔晚几秒，就会报出 `events=2 iProxy=-1` 这种看着像
  严重错位的失败。服务端其实早就广播了。已改为"两条视图都到位"再断言 ——
  这也与本文件开头「断言一律用等条件成立（waitFor），不要睡固定时间」的原则一致。

**验证**：本地模拟服务（`OFFLINE_MS=2000`，所有断言都会真跑）**59/59 全过**。
线上复跑仍有偶发失败，但特征已换成纯环境噪声 —— `read ECONNRESET`、连接超时，
以及随机房间号撞上此前测试遗留的房间（HTTP 409，脚本没有撞号重试）——
与服务端和游戏无关。

---

## v2026.09.23 · 弹层标题粘顶 + 项目展示与素材

### 变更

- **人数上限放宽**：本地传阅从 2–6 人扩到 **1–15 人**（1 人即单人挑战）；联机房间上限设为 **20 人**（服务端兜底）。
- **README 与仓库简介同步人数**（改代码时漏了，用户指出后补）：本地 1–15 人、联机上限 20 人。
  实测各人数的体验（每档 40 局平均）：

  | 人数 | 人均博几把 | 「一份都没博到」的人 |
  |---|---|---|
  | 6 | 38 把 | 0.0% |
  | 10 | 24 把 | 0.0% |
  | **15** | **17 把** | **0.2%** |
  | 20 | 11 把 | 1.0% |
  | 30 | 8 把 | 4.3% |
  | 50 | 5 把 | 14.5% |

  结论：**15 人以内在体验上完全没问题**；20 人可接受；30 人以上会有人整局一份都博不到（奖项共 63 份，
  人多就摊薄了）。技术层面服务端没有人数硬限制，跑 50 人也没问题——**限制来自游戏性，不是性能**。

### 修复

- **弹层（规则 / 战绩 / 概率 / 结算）右上角的「×」不再跟着内容滚走**：弹层内容区是滚动容器，
  标题原本在滚动区内部，内容一长「×」就滚出视野。改为标题粘在容器顶部
  （sticky + 负 top 抵消内边距 + 自带背景色），四个弹层共用样式一并修好。

### 仓库展示

- **补全 GitHub 仓库信息**（此前搜索结果里只有仓库名，简介一行空白）：
  - **简介**：闽南中秋博饼的网页版：六颗骰子丢进大红碗博状元，单文件零依赖、打开即玩。
    支持 1–15 人同机传阅、跨设备联机（自建房间服务·掉线自动托管）与挂机自动模式。
  - **主页入口**：<https://woo3an.top/bobing/>（仓库页右侧显示为可直接点开的链接）
  - **标签**：`bobing` `mid-autumn` `dice-game` `minnan` `h5-game` `multiplayer`
    `cloudflare-workers` `toy-project`
- 为此装了 GitHub CLI（隔离目录，不改系统环境），后续仓库信息可直接改。

### 文档 / 素材

- **预览图重做**：原来的 1200×630 预览图只截了第一屏，碗太大、**骰子和右侧战绩都被切掉**。
  新做法是截图前把页面缩一档，让「碗 + 六颗骰子 + 奖项 + 4 人战绩 + 记录 + 掷骰按钮」全部进框；
  另新增**手机端截图**展示响应式布局。
  · `preview.png` 1200×630（社交卡片 og:image 规范尺寸）
  · `preview-full.png` 1200×860（README 首图）
  · `preview-mobile.png` 390×844（手机端）
- **README 展示升级**：首图换为完整大图，并接入**自动模式动图**（骰子在碗里翻滚、战绩实时累计）
  —— GIF 之前只生成了文件、没在 README 里引用过。

---

## v2026.09.22 · 版本号修正

- `NET_BUILD` 从 `v2026.09.19a` 更新到 `v2026.09.22`——它 9/20（观战模式）、9/21（顶栏图标化）
  两次发布都没动过，而它是用户确认"WebView 没跑旧缓存"的**唯一凭据**。
- 另附：**随机性/公平性检查结论**（骰子 600 万颗卡方 3.59 ✓ 等概率；各座次状元率无显著差异；
  得奖总数有约 0.5%/局的先手偏差，属轮转制固有，**决定不修**）。

---

## v2026.09.21 · 顶栏一行化 + 状元结算口径

> 版本号（联机面板底部）已更新为 **v2026.09.22**，与本次发布对齐。

### 新增

- **页脚→工具栏的仓库入口**：顶部工具栏加入 GitHub 圆形图标（新窗口打开仓库），桌面/手机都常驻
- **手机端顶栏一行化**：音效按钮在窄屏改为**喇叭图标**（开着是「喇叭 + 声波」并高亮，关掉是「喇叭 + 斜杠」；
  桌面端仍显示「音效 开 / 音效 关」文字），配合月标与间距微调——
  **360px（安卓主流）、375/390px（iPhone 标准版）到 430px 全部一行**

### 修复

- **结算面板的「状元易主次数」漏算了"自己降级被夺"**：原先只在"被别人博出更大的状元夺走"时计数，
  自己博出更小的状元导致降级、被前面的人拿走的情况没有计入。现在两条路径都算（同一计数器）

### 变更（博饼记录文案）

- **首次博出状元**：第二行改为「**博得状元！**」+ **状元名称简称**——如「博得状元！五子带三」
  （第一行仍是完整骰面描述，如「五粒五带三」）
- **更小的状元夺不动**：第二行写明双方名称——「「状元带十一」小于 A 的「五红带二」，状元保住了」
- **自己降级丢掉状元**：区分两种情况——对方「**更大**」或「**同级，先博者保留**」，并写明状元归谁

### 规则澄清（一直如此，这次写进记录与测试）

状元以**各人最后博出的**为准：自己再博出更小的会**降级**；降级后若已不如别人，状元**易主**；
若降级到与别人**同级**，则**先博者**保留（先博的若因此上位，也算一次易主）。

---

## v2026.09.20 · 观战模式补全

### 修复

- **两分钟断线后进不了观战**：昨天的观战只覆盖了「原身份重连」这条路径，而真机上更常见的是
  **关页面 / 切后台被杀**——此时浏览器里的身份标记也一起丢了，重开走的是「按名字认领」，
  而认领要求「同名**且还没彻底断线**」，被移出的人正好不符合 → 被挡在门外。
  现在认领路径也支持观战：找不到"还在玩"的同名座次时，允许**接管那个已被移出的同名座次**进来只看不玩。
  - 关键配套：接管时**不清掉该座次的托管标记**（清了就等于让他复活并重新参与）；
    连接建立时按超时状态判定为只读观战者，其任何操作服务端一律丢弃
  - 边界守住：同名但**在线**仍拒绝（防顶掉正在玩的人）、不同名的新面孔仍拒绝（防陌生人乱入）
- **加固一条线上偶发会崩掉测试脚本的老用例**（断线演练中 `events[2]` 空指针 → 加重试与越界保护）

### 验证

协议测试 **59 项**（新增：认领已移出座次放行观战 + 该观战者只读 + 不同名仍拒）、
浏览器 e2e 23 步、托管专项 9 步、全套探针 13 组 481 断言——本地与线上全部通过。

---

## v2026.09.19 · 联机托管大版本（自动处置 / 取消托管 / 观战）

### 新增

- **掉线 / 挂机全自动处置**：轮到某人 15 秒没动静（页面开着也算挂机）或直接掉线 5 秒，
  其他人自动替他博——真实掷骰、每 0.4 秒一把、最多 5 把；满 5 把后每轮 0.2 秒自动跳过。
  从**第一次被代博**起算，2 分钟内回来一切照常；超过则视为彻底断线。
- **取消托管**：被托管期间主按钮始终是「取消代博（n/5）/ 取消自动跳过 · 我回来了」，
  任何时候可点；点击即真正取消 + 按钮立刻变回正常掷骰（轮到自己时顺手博一把）。
  取消后再发呆 15 秒才会重新托管。
- **观战模式**：被移出本局的人重连不再被拒，而是放进来观战——牌局实时可见，
  但掷骰/跳过/取消全部无效（服务端丢弃）。座次与已博到的奖品保留，下一局照常参与。
- **全员掉线保护**：没有任何自由玩家时，服务端把全员标记为跳过阶段（只改状态、不造事件），
  界面显示「全员掉线 · 本局已停止，2 分钟后自动解散」——不再卡在「代博中」。
- **房间自动解散**：所有人都被移出后，由 Durable Object 的定时器兜底解散房间。
- **聪明的重连**：重连失败先查服务端原因，被移出/房间已散就直接停手并明确提示；
  2 分钟到点有 3 秒自检（到点立刻刷新按钮与提示，不等下一次广播）。
- **托管/取消专项验证页**：`/dev/__auto.html`，浏览器或手机打开即自动跑 9 步断言
  （被托管→按钮是取消入口→点击真取消→局面继续推进→跳过阶段→超时被移出→不能复活）。

### 修复

- **只有掉线者回来才开始代博**：15 秒无操作的人 socket 往往还挂着，服务端此前只认
  "socket 断开"→ 拒绝别人的代博请求。改为服务端记录每人**最后操作时间**，超时即可被代博。
- **取消后局面卡住**：取消托管不产生事件，自动循环的计时器没人重排——取消后立即恢复推进。
- **重新托管被静默拦截**：并发去重判据从"同座次连续事件"收窄为"**同座次且同类型**"，
  否则取消/跳过之后的重新代博会把自己拦掉，整局卡死。
- **跳过卡在「已请求跳过」**：代博曾刷新"最后活动时间"导致跳过被拒。改为代博不算玩家活动，
  且满 5 把后跳过直接放行。
- **自动跳过太慢**：满 5 把后的跳过从等 5 秒提速到 0.2 秒。
- **重连后快速回放动画**：断线期间错过的把次改为**静默快速同步**（一条汇总日志），
  直接定位到最新一轮；只差一把时仍正常带动画。
- **WebView 顽固缓存**：HTML 响应加 `no-store`，联机面板底部显示版本号供比对。
- 若干提示问题：被移出只点名一次、跳过提示合并为一条、被移出者跳过完全静默、
  全员掉线用独立提示（不再被取消按钮挡住）。

### 变更

- 服务端新增托管状态机：`auto[seat]={cnt,at}`（代博计数与 2 分钟起点）、
  `cancelled[seat]`（取消宽限）、`lastAct[seat]`（本人最后活动）。
- 事件幂等收窄为「同座次且同类型」，观战者消息一律丢弃，被移出者不能取消。
- 协议测试从 17 项扩充到 **57 项**（掉线托管 / 取消 / 观战 / 全员解散全覆盖）。

---

## v2026.09.18 · 断线托管与移动端体验

- **断线全自动处置 v1**：15 秒判定 → 代博 5 把（带标记）→ 满 5 改自动跳过；
  开始代博起 2 分钟没回来彻底断线（重连/认领均拒，奖品按已博到的结算，再来一局复活）。
- **15 秒无操作即托管**（socket 没断也能被代博）、本人取消托管（初版）、直接掉线 5 秒起步。
- 服务端 roll/skip 幂等与事件上限；按名字认领修 iOS 回房 403。
- **重连静默回放**：漏掉的把次不再刷日志/响音效，只留一条同步汇总。
- **收官轮描述消歧义**：博出最后一个奖项 → 从下家再博一轮，不就此结束；
  **夺状元描述双方带具体名称**（状元带几 / 五子带几 / 插金花）。
- **iOS 断线唤醒**：切回前台不等 3 秒定时器立即重连；hash 回房双保险。
- 探针脚本收进仓库并修 UD 路径（cygpath），断言数扩到 305。
- 免费额度精算表：瓶颈是 SQLite 行写（≈270 行/局 → ≈370 局/天上限），安全日用 250 局。

---

## v2026.09.17 · 联机正式版（自建房间服务）

- 联机从实验方案（PeerJS/CloudBase）全面切换到**自建 Cloudflare Worker + Durable Object**：
  WebSocket 常驻推送、零月费、每房间号一个 DO。
- **联机功能**：掉线跳过、断线自动重连、再来一局、身份持久化（PEER）、加入失败原因提示。
- **联机健壮性**：掉线按钮实时重渲染、跳过幂等、**房主顺位**（房主退出自动移交）、
  打完退出不解散、Local 传输层不再盲信 storage 旧值、大厅掉线提示、防冒名校验。
- **收官轮改为「从最后得奖者的下家起算，每人再各博一次」**；退出房间时点名提示其他人。
- **游戏地址**：woo3an.top/bobing（/ 302 重定向，主页留白）；GitHub Pages 双入口同一房间服务。
- **一键探针**：`tools/run_probes.sh` 一条命令跑完全套自检（含 520/360 窄视口）。
- 结算面板点「先看看」关闭后，掷骰按钮变成「看结算」入口（旧版关掉就回不去）。
- 域名接入：绑 routes 而非 custom_domain（根域已有停放记录）。

---

## v2026.09.16 · 视觉与玩法大版本

- **碗精细建模**：滚金边、单块内壁、外壁月牙、釉色更深更红、光亮陶瓷质感；
  骰子擦亮、修正骰子平面"压缩两次"的问题；碗与骰子统一到同一相机视角。
- **自动模式上线**：挂机博到本局结束，随时暂停 / 继续 / 停止，三档速度（快档跳过动画）。
- **玩法口径确定**：**状元以各人最后博出的为准**（自己再博出更小的也算降级）；
  **收官轮**：最后一个奖项有主后，从博出者的下家开始每人再博一次。
- 修三档速度无区别（等待必须从落定起算）；骰子落点铺满碗底；整体提速；活动区缩 10%。
- 修手机端自动栏重叠。
- 联机对战（实验性）：同机多标签页可玩，CloudBase 跨设备零费用预留。

---

## v2026.09.15 · 打磨与定名

- 正式定名**「中秋博饼」**（去掉「掷骰大碗」等旧名）。
- **黑六勃概率说明**：它是 4 种骰面（222222/333333/555555/666666）的合计概率。
- 修手机端「重开一局」按钮上下跳动；修手机滚动时骰子乱动。
- 碗高光改成物理正确的软高光；骰子边缘去锯齿；骰子改成真正的立方体（面更清晰）；
  碗与骰子统一相机、内腔挖深、碗底下沉收紧（不再像漏斗）。

---

## v2026.09.14 · 诞生

- **首发**：单文件零依赖的闽南中秋博饼网页小游戏——
  - 判定引擎：穷举 6⁶ = 46656 种骰面组合的精确规则（状元插金花 > 六抔红 > 遍地锦 >
    黑六勃 > 五红 > 五子登科 > 四点红 > 对堂 > 三红 > 四进 > 二举 > 一秀）
  - 真实物理骰子（弹跳 / 碰撞 / 撞碗壁，无穿模）+ 内联 SVG 陶瓷大红碗
  - WebAudio 现场合成的骰子撞陶瓷声（不依赖任何音频文件）
  - 状元可夺、奖项份数可自定义、本地传阅 2–6 人 / 单人挑战（人数上限后续已扩至 1–15 人）
  - 分享卡片（og:image / twitter 卡片）
- 上线 GitHub Pages。

---

## 运行成本

域名 `woo3an.top` ≈ ¥35/年（.top 续费价）；Cloudflare Workers + Durable Object 免费额度
（瓶颈是 SQLite 行写 10 万/天：正常局约 270 行、含托管约 285 行 ≈ **每天 300+ 局**）；
GitHub Pages 备用入口 ¥0。**合计约 ¥35/年**。
